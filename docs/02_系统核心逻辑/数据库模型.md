# 数据库模型

事实来源：`sql/pgsql17.sql` 和 PostgreSQL adapter/read/write 代码。

## Schema 总览

| schema | 作用 | SQL 位置 |
| --- | --- | --- |
| `archive` | Markdown 解析归档、历史快照和历史结构化表 | `sql/pgsql17.sql:23-356` |
| `ingest` | 外部消息接入、AI 识别、AI 调用日志、pending | `sql/pgsql17.sql:367-487` |
| `core` | 当前业务事实源 | `sql/pgsql17.sql:490-691` |
| `monitor` | GitHub Actions run/job/step/failure 监控事实 | `docs/03_历史重构记录/重构历史/action日志监控/03_github_action_monitor.sql`、`src/adapters/postgres/github-action-monitor-repository.pg.mjs` |
| `maintenance` | 显式迁移历史和维护元数据 | `sql/pgsql17.sql`、`sql/training_records/migrations/` |

## 权限与迁移边界

当前数据库边界按角色拆分，不再把 DDL 混入日常同步路径：

| 角色 / 连接 | 用途 | 边界 |
| --- | --- | --- |
| `training_migrator` / `TRAINING_DB_MIGRATION_URL` | 显式 migration | 执行 `sql/training_records/migrations/`，写入 `maintenance.schema_migration`；不注入日常同步 workflow。 |
| `training_app` / `TRAINING_DB_URL` | Telegram / 飞书同步和常规写入 | 只做业务 DML；默认不执行 `ALTER TABLE`、`CREATE TABLE`、`CREATE INDEX`。 |
| `training_maintenance` | 显式维护命令 | 用于受控修复、导入和 backfill；仍不替代 migrator 执行 DDL。 |
| `training_readonly` / `TRAINING_DB_READONLY_URL` | 站点构建、快照读取、导出、巡检和一致性检查 | 只读查询；未配置时读取路径才回退 `TRAINING_DB_URL`。 |

运行时代码不再包含 schema preflight。正式 schema 变更只能通过 `npm run maintenance:migrate -- --dry-run` 预览，并在配置 `TRAINING_DB_MIGRATION_URL` 后使用 `npm run maintenance:migrate -- --confirm` 执行。

## core 表

| 表 | 作用 | 写入代码 | 读取代码 |
| --- | --- | --- | --- |
| `core.training_day` | 日汇总 | `src/adapters/postgres/core-row-writer.pg.mjs`、`incremental-write.pg.mjs` | `src/db/training/read-queries.mjs` |
| `core.measurement` | 体脂/体测明细 | `core-row-writer.pg.mjs` | `read-queries.mjs`、`read-mapper.mjs` |
| `core.sleep` | 睡眠明细 | `core-row-writer.pg.mjs`、`src/db/training/write.mjs` sleep backfill | `read-queries.mjs`、`read-mapper.mjs` |
| `core.activity` | 活动明细 | `core-row-writer.pg.mjs`、`incremental-write.pg.mjs` | `read-queries.mjs`、`read-mapper.mjs` |
| `core.meal` | 饮食明细 | `core-row-writer.pg.mjs`、`incremental-write.pg.mjs` | `read-queries.mjs`、`read-mapper.mjs` |
| `core.thought` | 随想正文、模块、图片引用、软删除状态 | `src/adapters/postgres/thought-repository.pg.mjs` | `read-queries.mjs`、`read-mapper.mjs` |

## ingest 表

| 表 | 作用 | 代码 |
| --- | --- | --- |
| `ingest.source_batch` | 来源无关的 batch payload、hash 和处理状态 | `PostgresSourceBatchRepository.upsertBatch` |
| `ingest.source_message` | 消息及来源 identity | `PostgresSourceBatchRepository.upsertMessages` |
| `ingest.source_asset` | 图片/文档资源 identity 与非敏感元数据 | `PostgresSourceBatchRepository.upsertMessages` |
| `ingest.recognition_run` | 标准化识别、OCR/图片证据、缓存键和原始结果 | `PostgresSourceBatchRepository.upsertRecognitions` |
| `ingest.ai_call_log` | AI 调用审计 | `writeStartedRecognitionAiCallLog`、`upsertRecognitionAiCallLogs` |
| `ingest.pending_task` | 来源无关的待重试任务 | pending recognition / persistence replay 代码 |

旧 `ingest.telegram_batch/message/recognition/pending_batch` 仅作为 Phase 2 回填后的观察期历史表保留，生产代码已无访问。dev 已执行 `sql/migration.sql` 与 `sql/migration_phase2_generic_ingest.sql`；main 尚未执行。受保护 cleanup 只有在完整观察周期和计数验收后才允许人工执行。

## maintenance 表

| 表 | 作用 | 代码 |
| --- | --- | --- |
| `maintenance.schema_migration` | 记录显式 migration 文件名、checksum 和执行时间，阻断同名 migration 静默漂移。 | `tools/training-maintenance.mjs migrate` |

## monitor 表

`monitor` schema 保存 GitHub Actions 自身运行生命周期和系统参数健康检查结果，不保存业务 payload、聊天 ID、图片 key、Prompt、SQL 参数、完整 Action 日志正文、Secret 明文或参数值。Action 监控写入入口是 `tools/report-github-action-status.mjs` 和 `tools/github-action-monitor-server.mjs`；参数健康写入入口是 `tools/check-parameter-health.mjs`。读取入口是站点构建中的 `loadActionMonitorViewFromPostgres()`。

| 表 | 作用 | 写入 / 读取代码 |
| --- | --- | --- |
| `monitor.github_action_runs` | workflow run 主表，每个 `run_id` 一行；保存 workflow、branch、commit、actor、status、conclusion、耗时、URL 和失败摘要。 | `src/adapters/postgres/github-action-monitor-repository.pg.mjs` |
| `monitor.github_action_jobs` | job 明细，每个 GitHub job id 一行；保存 job 状态、结论、runner、labels、耗时和 URL。 | `src/adapters/postgres/github-action-monitor-repository.pg.mjs` |
| `monitor.github_action_steps` | step 明细，每个 `job_id + step_number` 一行；保存 step 名称、状态、结论和耗时。 | `src/adapters/postgres/github-action-monitor-repository.pg.mjs` |
| `monitor.github_action_failures` | 失败摘要表；按 run/job/step 层级记录失败 `failure_key`、结论、摘要和安全上下文。 | `src/adapters/postgres/github-action-monitor-repository.pg.mjs`、`src/app/use-cases/github-action-monitor.use-case.mjs` |
| `monitor.system_config_parameters` | 系统参数健康主表；保存参数名、位置、分类、有效期规则、来源文档和非敏感元数据，不保存参数值。 | `src/adapters/postgres/parameter-health-monitor-repository.pg.mjs` |
| `monitor.system_config_parameter_checks` | 参数健康检查历史；保存 `healthy`、`present`、`invalid`、`missing`、`not_configured`、`unreachable`、`unsupported`、`unknown`、探测类型、耗时、失败分类和可选真实到期日。 | `src/adapters/postgres/parameter-health-monitor-repository.pg.mjs` |

`monitor_environment` 标记写入实例环境，dev 库写 `dev`，main 库写 `main`。仓库层兼容旧表缺少 `monitor_environment` 列的情况：写入或读取遇到 `42703 monitor_environment` 时，会回退到无该列的 SQL，并按 `branch` 过滤历史数据。

参数健康 registry 位于 `config/parameter-health/dev.json` 和 `config/parameter-health/main.json`。根级 `probes` 定义只读探测，参数通过 `healthProbeKey` 引用。数据库升级由 `sql/training_records/migrate_parameter_health_monitor.sql` 手工执行；代码不会自动修改生产表。

参数健康相关表的字段边界：

| 表 | 字段边界 |
| --- | --- |
| `monitor.system_config_parameters` | 保存参数身份、`health_probe_key`、`health_check_type`、来源信息和可选到期元数据；probe 的环境变量映射只在 registry 中维护。 |
| `monitor.system_config_parameter_checks` | 保存健康状态、`check_type`、`latency_ms`、`failure_kind`、`observed_expires_at`、证据来源、安全提示和非敏感 details；`last_healthy_at` 在查询时从历史计算。 |

这两张表不保存参数值、value hash、DB URL、token、API key、聊天 ID、COS key 或完整 provider 响应。`source_doc` / `source_code_json` 只用于追溯参数来自哪个文档、workflow 或代码入口。

## archive 表

| 表 | 作用 |
| --- | --- |
| `archive.training_parse_snapshot` | 训练记录 Markdown 解析快照。 |
| `archive.training_parse_run` | 解析运行历史。 |
| `archive.training_day` | 历史日汇总。 |
| `archive.training_activity` | 历史活动明细。 |
| `archive.training_measurement` | 历史体测明细。 |
| `archive.training_meal` | 历史饮食明细。 |
| `archive.training_sleep` | 历史睡眠明细。 |

## 幂等

| 对象 | 幂等依据 |
| --- | --- |
| batch | `persistNormalizedBatch` 对完整 batch JSON 计算 SHA-256 `payloadHash`。 |
| activity | `core-row-writer.pg.mjs` 使用 `sourceChannel + date + time + type + detail` 生成 hash/key。 |
| meal | `core-row-writer.pg.mjs` 使用日期和餐次字段生成 key。 |
| sleep | `core-row-writer.pg.mjs` 使用睡眠字段生成 key。 |
| thought | `core.thought` 有 source identity 唯一索引，SQL 位置 `sql/pgsql17.sql:679-681`。 |
| action run | `monitor.github_action_runs.run_id`。 |
| action job | `monitor.github_action_jobs.job_id`。 |
| action step | `monitor.github_action_steps` 的唯一约束 `job_id + step_number`。 |
| action failure | `monitor.github_action_failures.failure_key`，写入当前 run 前先删除该 run 的旧 failures 再插入最新失败集合。 |
