# 故障排查

本文是 AI、GitHub Actions、数据库、Telegram、飞书、Cloudflare Worker、数据质量和站点/CDN 故障排查的唯一入口。


## 故障排查总览

先判断故障发生在哪一层：

1. 通道入口。
2. Cloudflare Worker。
3. GitHub Actions。
4. AI。
5. 数据库。
6. Markdown backup。
7. Pages/CDN。

## 专题

- [AI 异常](#ai-异常)
- [GitHub Actions 异常](#github-actions-异常)
- [数据库异常](#数据库异常)
- [Telegram 异常](#telegram-异常)
- [飞书异常](#飞书异常)
- [Cloudflare Worker 异常](#cloudflare-worker-异常)
- [数据质量问题](#数据质量问题)
- [站点与 CDN 问题](#静态站点与-cdn-问题)


## AI 异常

## 排查顺序

1. `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`。
2. timeout、429、5xx、网络错误。
3. 是否触发 fallback。
4. JSON/schema parse failure。
5. 低置信度或业务拒绝。
6. `ingest.ai_call_log`。

## 注意

低置信度和缺少可靠日期不是平台故障，不应通过无限重试解决。

AI 主备容灾演练见 [AI 主备容灾演练](../AI识别体系/AI识别体系.md#ai-主备容灾演练)。


## GitHub Actions 异常

## 排查顺序

1. workflow 是否启动。
2. run 是否 cancelled、failed、timed out。
3. `queue_task_id` 是否能匹配 run。
4. summary 是否显示业务失败。
5. commit/push/deploy 是否失败。

## 关键区别

- Action failed：执行失败。
- Action success + `partialFailure`：业务部分失败。
- Action success + `pending_replay`：已进入补偿。

## 队列失败

排查 `queue_task_id`：

1. 在 Worker/Queue 日志中查 `queue_task_id`。
2. 在 workflow run name 或 dispatch payload 中查同一 ID。
3. 若 workflow run 查找超时 / run lookup timeout，查 `SyncDispatchQueue` dead-letter。
4. 如果需要重跑 / rerun，使用原始 payload，避免改变 `payload_hash`。

同一批次重复执行时，数据库应根据 `batch_id + payload_hash` 返回 `unchanged`，不得重复写 core。

## Markdown Backup

`markdown-backup.yml` 若出现 `changed_without_commit`，表示导出发生变化但未提交。若 workflow 在 summary 前失败，可按 `workflow_failed_before_alert_evaluation` 处理，直接看 job 日志和导出步骤。


## 数据库异常

## 表现

- `persistenceStatus=pending_replay`
- `failureCategory=database`
- `ingest.telegram_pending_batch` 有 pending
- 构建读取 database 失败

## 处理

1. 检查 `TRAINING_DB_ENABLED` 和 DB URL。
2. 检查网络、SSL、权限、schema。
3. 数据库恢复后重放 pending。
4. 运行一致性检查。

恢复步骤见 [数据恢复](../运维手册/运维手册.md#数据恢复)，常用查询见 [索引与查询手册](../数据模型/查询手册.md)。


## Telegram 异常

## 常见问题

- webhook URL 错误。
- secret header 不一致。
- Bot token 错误。
- chat 未授权。
- 图片以 photo 发送导致无原始文件名。
- 文件下载失败。

## 快速判断

发送 `/help`。若无回复，先查 Worker 和 webhook；若有回复但同步失败，查 Actions summary。


## 飞书异常

## 常见问题

- Request URL 指向错误环境。
- Encrypt Key / Verification Token 不一致。
- App 权限未发布。
- `FEISHU_ALLOWED_CHAT_IDS` 未包含目标 chat。
- 图片下载缺 `im:resource`。
- App ID / Secret 错误。

## 快速判断

先验证 URL challenge，再发送 `/帮助`，最后发送测试随想或带日期图片。


## Cloudflare Worker 异常

## 常见问题

- Worker 未部署。
- route 或 Request URL 指错。
- Durable Object binding 缺失。
- `GITHUB_TOKEN` 缺失或权限不足。
- GitHub dispatch 失败。
- run lookup timeout。
- dead-letter。

## 工具

```bash
npx wrangler tail <worker-name> --format=pretty
```


## 数据质量问题

## 常见问题

- 图片日期不可靠。
- 睡眠跨日归档误读。
- 同批多日期冲突。
- AI 识别字段为空但被误判成功。
- core 明细存在重复或来源丢失。

## 判断

不要只看最终 `archivedDate`。同时查看：

- `dateSources`
- `warnings`
- `confidence`
- `source_channel`
- `core.*` 明细行
- `ingest.*` 原始 payload


## 静态站点与 CDN 问题

## 页面没更新

1. 数据是否写入 `core.*`。
2. build:data 是否读取 database。
3. Pages workflow 是否运行。
4. artifact 是否上传。
5. Cloudflare 缓存是否命中旧内容。

## CDN

生产域名经 Cloudflare 代理时，CSS/JS/图片/HTML 可能有不同缓存策略。必要时清理缓存。
