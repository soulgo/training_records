# Action 日志优化文档最终验收报告

> 审核日期：2026-06-28
> 审核口径：源码、workflow、SQL schema 为唯一事实源；历史 Action 样本日志不作为结论依据。
>
> **第三轮修订（2026-06-28）**：基于真实运行日志复核，原报告存在一处关键误判——dispatch payload 泄漏被错列为"不成立问题"，实为生产 main 分支真实发生的安全阻塞项。带 ⚠️ 的条目为第三轮修订。

## 本次复核范围

本次复核覆盖 `docs/后续规划_未实现/action 日志排查优化/` 下 01-05 全部文档，并重新阅读以下代码：

- GitHub Actions：`.github/workflows/sync.yml`、`.github/workflows/sync-dev.yml`、`.github/actions/site-build/action.yml`、`.github/workflows/deploy-pages.yml`、`.github/workflows/deploy-cloudflare-pages-dev.yml`、`.github/workflows/markdown-backup.yml`、`.github/workflows/ci-tests.yml`
- Cloudflare：`cloudflare/sync-dispatch-worker.mjs`、`cloudflare/telegram-sync-dispatch-worker.mjs`、`cloudflare/feishu-sync-dispatch-worker.mjs`、`cloudflare/sync-dispatch-queue.mjs`
- Action 同步链路：`src/app/use-cases/telegram-sync.use-case.mjs`、`src/app/use-cases/feishu-sync.use-case.mjs`、`src/app/use-cases/telegram-sync/image-processing.mjs`、`src/app/use-cases/telegram-sync/status.mjs`
- AI：`src/app/use-cases/image-recognition.use-case.mjs`、`src/adapters/ai/openai-compatible.adapter.mjs`、`tools/training-analysis.mjs`、`tools/training-analysis-request.mjs`
- DB：`src/db/training/write.mjs`、`src/db/training/pending-recognition.mjs`、`src/db/training/read-client.mjs`、`src/adapters/postgres/telegram-batch-repository.pg.mjs`、`src/adapters/postgres/incremental-write.pg.mjs`
- 外部模块：`src/adapters/telegram/*`、`src/adapters/feishu/*`、`tools/telegram-thoughts.mjs`
- Markdown / 运维：`tools/export-training-markdown.mjs`、`tools/training-maintenance.mjs`
- Schema：`sql/pgsql17.sql`、`sql/training_records/ingest.sql`

## 审核结论

**文档层：通过。**
6 份文档已按源码事实重写或修正，当前可作为后续日志治理实施依据。

**系统层：有条件通过。**
业务同步链路没有因日志问题阻塞运行，但当前日志体系尚未达到“完整日志治理上线标准”。阻塞项是 `export:markdown` 在 Action 中输出完整训练 snapshot。

## 真实成立的优化项

| 优化项 | 代码依据 | 结论 |
| --- | --- | --- |
| P0：`export:markdown` compact summary | `export-training-markdown.mjs:47-51` 返回 `snapshot`；`training-maintenance.mjs:87` 输出 pretty JSON；Action 中调用 `npm run export:markdown` | 保留 |
| traceId 贯穿 | 源码无 `traceId`；Queue 只传 `queue_task_id`，见 `sync-dispatch-queue.mjs:451-485` | 保留 |
| AI summary 补 provider/model/promptVersion/token | 识别结果和 DB call log 有字段，但 summary 只显示 `aiAttemptKinds` | 保留 |
| DB summary 补 row count / transaction / pending / rollback | `write.mjs:41-151` 成功返回缺少 row count 和 transaction id | 保留 |
| DB slow query wrapper | DB 直接 `client.query(...)`，无 threshold wrapper | 保留 |
| deploy wait 状态日志 | `sync.yml:570-624`、`sync-dev.yml:556-610` 轮询中缺 periodic status | 保留 |
| sync summary 脚本抽取 | main/dev、Telegram/Feishu summary 都是 inline Node 重复逻辑 | 保留 |
| Worker 正常路径 trace | Worker/Queue 成功路径默认少日志；失败/dead-letter 才明显记录 | 保留 |
| CI 降噪与去重 | `ci-tests.yml` schedule/manual 下 fast 和 full test 重叠 | 保留 |

## 已修正的问题

| 原文问题 | 修正后状态 |
| --- | --- |
| 将“原始 dispatch payload 已进入普通 Action 日志”作为 P0 事实 | ⚠️ **第三轮回退**：原"已改为未 echo 原文"的修正**错误**。实测生产 main 仍写 `SYNC_DISPATCH_PAYLOAD` 原文到 `$GITHUB_ENV`（Sync Main #112 日志可见）。已回退为 P0 阻塞项（见 P0-A），须合并 dev 修复 |
| 把 AI 调用日志描述为“记录 token / prompt” | 已改为：token usage 有字段，prompt 全文不记录，只记录 `promptVersion` |
| 将异常处理概括为“异常吞掉” | 已改为：区分 critical path、pending replay、best-effort、fallback 分支 |
| 将日志等级错误作为确定结论 | 已改为：当前没有统一 level 体系，只能确认 stderr/plain text/annotation/prefix 混用 |
| 使用历史 run 行数和耗时作性能结论 | 已删除，改为源码可验证的等待循环、重复安装、stdout 过量、DB client 分散 |
| 行号指向声明行或旧位置 | 已统一改为当前源码中的函数/语句区间 |

## 被删除的不成立问题

| 被删除问题 | 删除原因 |
| --- | --- |
| 同一批次无条件重复调用 AI | 只发现 HTTP retry、response_format fallback、provider fallback 等有条件机制 |
| COS 重复上传 | Tencent COS 上传前 `HeadObject`，已存在则 `skipped`；失败后也会再次确认对象是否已存在 |
| 历史样本日志行数可作为当前文档结论 | 当前仓库无法从源码复算历史 Action 样本日志 |
| ⚠️ **第三轮回退**：~~workflow 已把 `DISPATCH_PAYLOAD` / `SYNC_DISPATCH_PAYLOAD` 原文写入 `$GITHUB_ENV`~~ | **原删除错误**。实测生产 main 仍写入（`sync.yml:89-91`、`sync-dev.yml:83-85`），Sync Main #112 日志第 229/605/697/828 行可见原文泄漏。dev 已修复（`026ef49`）未合并 main。此项**回退为 P0 阻塞项**，不得删除 |

## 系统当前真实状态总结

| 检查项 | 当前真实状态 |
| --- | --- |
| TraceId 是否贯穿全链路 | 否。只有 `queue_task_id`、batch/task id、`aiCallId` 等局部关联 |
| Action 生命周期日志是否完整 | 部分完整。workflow step、summary、timings 存在；缺统一 started/completed 结构化日志 |
| AI 调用是否记录 token / prompt | token usage 有 DB 字段；prompt 全文不记录；Action summary 不展示 token |
| DB 是否有慢查询日志 | 否 |
| 是否存在日志重复打印 | 存在字段级重复和 YAML summary 逻辑重复，不是同一行重复刷屏 |
| 是否存在异常吞掉 | 存在 best-effort / fallback；关键写库失败不是静默吞掉 |
| 是否存在日志污染 | 是，主要是 Markdown full snapshot、file_id/image_key、chatIds/sourceId、COS bucket/pathPrefix/key |
| 是否存在性能浪费 | 有重复 npm ci、deploy wait、DB 多 client；重复 AI/COS 未成立 |
| 是否存在日志等级错误 | 无统一等级体系，不能按正式 level 错误下结论 |

## 是否达到可上线标准

| 对象 | 判断 | 说明 |
| --- | --- | --- |
| 文档作为实施依据 | 达到 | 6 份文档已按源码事实重写，A/B/C 分类明确 |
| 业务同步系统 | 有条件通过 | 日志问题不阻塞当前同步业务运行 |
| 日志治理体系 | 未达到完整上线标准 | `export:markdown` full snapshot 进入 Action stdout 是阻塞项；trace/AI/DB summary 仍未实施 |

## 是否存在阻塞性问题

存在 2 个日志治理阻塞项：

- **P0-A：dispatch payload 泄漏（生产 main 分支，已真实发生）**——`main` 的 `sync.yml`/`sync-dev.yml` 仍把完整 `SYNC_DISPATCH_PAYLOAD`（含 `file_id`/`from.id`/`username`/`chat.id`）写入 `$GITHUB_ENV` 并重复打印。实测 Sync Main #112 日志可见。修复 `026ef49`+`2d45b74` 仅在 dev，须合并 main。⚠️ 本项为第三轮新增，原报告漏列。
- **P0-B：`export:markdown` snapshot 泄漏（backup + 两个 deploy）**——在 deploy / markdown-backup Action 中输出包含完整 `snapshot` 的 payload。实测 Deploy #350 含 399 处、Deploy CF #233 含 223 处、Backup #20 含 1746 行健康明细（`weightKg`/`bodyFatPct`/`sleepStageText`/`analysisText`）。⚠️ 第三轮将范围从"仅 backup"扩大到"backup + 两个 deploy，且 deploy 为高频"。

不存在以下阻塞项：

- 未发现 Secret、API Key、DB URL 被源码主动打印。
- 未发现 workflow 当前直接打印原始 dispatch payload。
- 未发现同一批次无条件重复 AI 调用或 COS 上传。

## 是否需要再次审计

需要，但不是立刻重复本轮文档审计。

建议在完成 P0/P1 代码实施后再次审计：

1. 用最新一次 dev/main Action run 验证日志中不再出现完整 `snapshot` 和健康明细。
2. 验证 summary 中能看到 traceId、AI provider/model/token、DB status/row count、deploy duration。
3. 验证新增 logger 没有打印 prompt 全文、用户原文、file_id/image_key、chat_id、COS key。
4. 验证 failure monitor 在失败场景仍能通知 Telegram / 飞书目标。

## 最终结论

本次“源码级一致性校验”已完成。当前 01-05 文档中的结论已回到源码事实，06 已重写为验收报告。后续实施应按 `05_最终实施建议.md` 的顺序推进：先修 Markdown compact summary，再做 trace、summary 抽取、AI/DB/deploy 结构化日志。
