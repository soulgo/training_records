# 代码与日志对应关系

## 说明

本文建立 Workflow -> Job -> Step -> 代码文件 -> 当前日志输出 -> 优化方向 的对应关系。所有问题描述均按当前源码复核，不引用历史 Action 样本日志。

> **第三轮修订（2026-06-28）**：引入分支基线。生产 `main` 与 `dev` 已分叉——dispatch payload 泄漏在 main 真实发生（dev 已修复未合并）。带 ⚠️ 的条目为第三轮基于真实运行日志的修订。

## 全链路总图

```text
Telegram / 飞书
-> cloudflare/sync-dispatch-worker.mjs
-> TelegramAlbumBuffer / FeishuImageBuffer
-> SyncDispatchQueue
-> sync.yml / sync-dev.yml
-> npm run sync:telegram / sync:feishu
-> runMessageSync()
-> recognizeBatch()
-> AI provider
-> persistNormalizedBatch()
-> deploy-pages.yml / deploy-cloudflare-pages-dev.yml
-> site-build action
-> npm run export:markdown
-> npm run build
-> Pages deploy
```

系统文档对应：`docs/消息链路/消息链路.md`、`docs/部署运维/部署运维.md`。

## Cloudflare Worker

| 环节 | 代码 | 当前日志 | 问题分类 | 优化方向 |
| --- | --- | --- | --- | --- |
| 统一入口 | `cloudflare/sync-dispatch-worker.mjs:28-52` | 仅 5xx 时 `[sync-dispatch-worker]` | A：正常路径缺少 accepted trace | 增加 `worker.webhook.accepted` 安全摘要 |
| Telegram webhook | `cloudflare/telegram-sync-dispatch-worker.mjs:114-233` | 成功只返回 JSON；dispatch 正常路径无日志 | A：无法从 Worker tail 确认正常 enqueue/dispatch | 输出 channel、updateCount、imageCount、traceId |
| Telegram buffer | `cloudflare/telegram-sync-dispatch-worker.mjs:24-111` | 仅通知失败时 `[telegram-album-buffer]` | B：buffer 成功 flush 不记录 queue task | flush 时输出 traceId / payloadHash |
| 飞书 webhook | `cloudflare/feishu-sync-dispatch-worker.mjs:121-247` | `[feishu-webhook]` 输出 event/chat/message | A：chat/message 原文可识别；⚠️ **第三轮补强**：sync stdout 的 result JSON（batchId/sourceId/chatIds）也明文含 `oc_` chat id（实测 Sync Dev #117:343/346/351） | hash chat id，保留 event id；result JSON 同步脱敏 |
| 飞书 buffer | `cloudflare/feishu-sync-dispatch-worker.mjs:23-119` | `[feishu-message-buffer]` buffered/flushed/retry/dead_letter | B：格式与 Telegram 不一致 | 统一 `QUEUE`/`WORKER` domain |
| 队列入队 | `cloudflare/sync-dispatch-queue.mjs:224-246` | 成功默认无日志 | A：无法从 Worker 日志确认 enqueue | 输出 `queue.task.enqueued` |
| workflow dispatch | `cloudflare/sync-dispatch-queue.mjs:369-385` | 失败进入 retry/dead-letter，成功无日志 | A：dispatch 成功不可见 | 输出 workflowFile、ref、queueTaskId、traceId |
| run lookup | `cloudflare/sync-dispatch-queue.mjs:387-423` | timeout 后 dead-letter | A：中间轮询不可见 | 输出 periodic wait status |

## Sync (Main)

Workflow: `.github/workflows/sync.yml`

| Job | Step | 代码位置 | 调用代码 | 当前日志 | 优化方向 |
| --- | --- | --- | --- | --- | --- |
| `sync` | Checkout main branch | `sync.yml:52-56` | `actions/checkout@v4` | 标准 action logs | 保留 |
| `sync` | Setup Node.js | `sync.yml:58-62` | `actions/setup-node@v4` | 标准 action logs | 保留 |
| `sync` | Install dependencies | `sync.yml:64-66` | `npm ci` | npm 输出全部进入日志 | routine run 可加 `--no-audit --fund=false`，审计独立化 |
| `sync` | Determine sync channel | `sync.yml:68-115` | inline Node 解析 `dispatch_payload` | 只输出 event action；⚠️ **第三轮修正：main 分支仍写 `SYNC_DISPATCH_PAYLOAD` 原文到 `$GITHUB_ENV`**（dev 已改只写 `SYNC_DISPATCH_EVENT_PATH`） | 合并 dev 修复到 main；保持不打印 payload；增加安全摘要 |
| `sync` | Sync safe database repairs | `sync.yml:117-119` | `npm run sync:db` | 仅非 webhook Telegram 跑 | 增加 maintenance summary |
| `sync` | Sync updates | `sync.yml:121-183` | `npm run sync:telegram` / `sync:feishu` | 输出同步 JSON、timings、AI/DB/COS stderr | 使用统一 logger；stdout compact report |
| `sync` | Detect changes | `sync.yml:185-258` | inline Node 读 result file | 输出 `readyStoredContentBatches`、thought check | 保留，补 traceId 和 result path 是否存在 |
| `sync` | Run tests | `sync.yml:260-263` | `npm run test:fast` | 仅部分条件运行 | 测试 stderr 降噪 |
| `sync` | Commit/Rebase/Push | `sync.yml:265-284` | git | 标准 git 输出 | 失败时输出 changed file count |
| `sync` | Write Telegram sync summary | `sync.yml:286-394` | inline Node + `tools/telegram-sync.mjs` | Markdown 表格，含 timings、batch 状态、image storage | 抽为 `tools/action-sync-summary.mjs`，补 AI/DB/trace 字段 |
| `sync` | Write Feishu sync summary | `sync.yml:396-498` | inline Node + `tools/feishu-sync.mjs` | 与 Telegram 逻辑重复，额外输出 `sourceId/chatIds` | 与 Telegram 共用脚本，hash chatIds |
| `sync` | Notify Telegram sync result | `sync.yml:500-508` | `tools/telegram-sync-notify.mjs` | JSON `{notified,sent}` | 增加 `NOTIFY` domain，记录失败原因 |
| `sync` | Notify Feishu sync result | `sync.yml:510-519` | `tools/feishu-sync-notify.mjs` | compact JSON | 统一字段 |
| `sync` | Trigger and wait for site deploy | `sync.yml:521-629` | GitHub API curl | 只输出 run id 和最终结果 | 增加周期性 wait 日志和 deploy duration |
| `sync` | Failure notify | `sync.yml:631-660` | action monitor scripts | 失败时通知 | 补 traceId；继续避免 payload 输出 |

## Sync (Dev)

Workflow: `.github/workflows/sync-dev.yml`

| Job | Step | 代码位置 | 调用代码 | 当前日志 | 优化方向 |
| --- | --- | --- | --- | --- | --- |
| `sync` | Checkout dev branch | `sync-dev.yml:46-50` | `actions/checkout@v4` | 标准 action 输出 | 同 main |
| `sync` | Install dependencies | `sync-dev.yml:58-60` | `npm ci` | npm 输出全部进入日志 | 同 main |
| `sync` | Determine sync channel | `sync-dev.yml:62-109` | inline Node | 只写临时 event path | 保持不打印 payload；增加安全摘要 |
| `sync` | Sync updates | `sync-dev.yml:111-183` | `npm run sync:feishu` 等 | 含 COS 隔离校验 | 保留隔离 error，其他输出标准化 |
| `sync` | Detect changes | `sync-dev.yml:185-256` | inline Node | 输出 content changed | 补 traceId |
| `sync` | Commit/Push | `sync-dev.yml:258-270` | git | 标准 git 输出 | 保留 |
| `sync` | Write summaries | `sync-dev.yml:272-484` | inline Node | 与 main 重复 | 抽公共脚本 |
| `sync` | Trigger async dev deploy | `sync-dev.yml:507-615` | GitHub API curl | run id + 最终结果 | 补周期性状态 |
| `sync` | Failure notify | `sync-dev.yml:617-643` | monitor scripts | 失败时通知 | 补 traceId |

## 同步脚本与 use case

| npm script | package 位置 | 入口 | 当前输出 | 优化方向 |
| --- | --- | --- | --- | --- |
| `sync:telegram` | `package.json:26` | `telegram-sync.use-case.mjs:79-83` | pretty JSON report + stderr timings | stdout compact report，summary 文件保留完整安全字段 |
| `sync:feishu` | `package.json:29` | `tools/feishu-sync.mjs:16-20` -> `feishu-sync.use-case.mjs:29-32` | pretty JSON report | 与 Telegram 使用同一 report schema |
| `telegram:webhook` | `package.json:28` | `tools/telegram-webhook.mjs` | webhook result JSON | 保留，隐藏 token |
| `export:markdown` | `package.json:13` | `training-maintenance.mjs:672-696` | 当前输出包含完整 `snapshot` | P0：只输出 compact export summary |
| `maintenance:inspect` | `package.json:17` | `training-maintenance.mjs:91-120` | 监控 JSON | 保留，注意不输出敏感明细 |

## AI 识别模块

| 环节 | 代码 | 当前日志 | 优化方向 |
| --- | --- | --- | --- |
| 加载 prompt metadata | `image-processing.mjs:32-35` | 无 | 输出 promptVersion/schemaVersion 到安全 summary |
| cache read failed | `image-recognition.use-case.mjs:102-106` | `[telegram-sync] recognition cache read failed ...`，含 fileUniqueId | 改结构化 `AI cache.read.failed`，fileUniqueId hash |
| primary request | `image-recognition.use-case.mjs:321-350` | request started 只写 DB best-effort，不进 Action summary | 增加 request started/completed |
| response_format fallback | `image-recognition.use-case.mjs:788-821` | 无结构化 attempt 日志 | 输出 attemptKind |
| fallback provider | `image-recognition.use-case.mjs:196-244` | `primary AI recognition failed...` | 输出 provider/model/fallbackUsed |
| HTTP retry | `openai-compatible.adapter.mjs:115-145` | plain text retry | 统一 retry schema |
| AI call log started | `image-recognition.use-case.mjs:824-861` | 失败才输出 | 成功也在 summary 显示 aiCallLogStatus |
| AI call log DB 写入 | `pending-recognition.mjs:223-253`、`telegram-batch-repository.pg.mjs:164-230` | DB 成功默认无日志 | batch summary 聚合写入状态 |

## 数据库写入

| 环节 | 代码 | 当前日志 | 优化方向 |
| --- | --- | --- | --- |
| 读取 pending | `pending-recognition.mjs:13-86` | 无成功日志 | 输出 claimed count、oldest age、duration |
| append pending | `pending-recognition.mjs:135-221` | 失败由调用方或 AI call log best-effort 记录 | 输出 queued/resolved/attemptCount/nextRetryAt |
| batch persist | `write.mjs:41-151` | 成功无日志；AI call log 失败和 rollback 失败才日志 | 输出 transaction start/end、status、duration |
| schema preflight | `write.mjs:73`、`export-training-markdown.mjs:58-93` | export preflight transient retry 有日志 | 输出 preflight duration 和 retry count |
| ingest upsert | `telegram-batch-repository.pg.mjs:13-160` | 无 row count summary | 输出 row counts 汇总 |
| core incremental write | `incremental-write.pg.mjs:15-36` | 无 | 输出 affected dates 和 row counts |
| DB snapshot parallel read fallback | `read-client.mjs:37-57` | `[training-db-read] parallel ... retrying with one client` | 保留，补 query group 和 duration |
| 慢查询 | 无统一 wrapper | 无 | 新增 threshold-based slow query log |

## Markdown 与站点构建

| Workflow | Step | 代码 | 当前日志 | 优化方向 |
| --- | --- | --- | --- | --- |
| Shared Site Build | Export database markdown | `.github/actions/site-build/action.yml:109-123` | `npm run export:markdown` 输出完整 payload | compact export summary |
| Shared Site Build | Sync safe DB repairs | `.github/actions/site-build/action.yml:125-128` | `npm run sync:db` | maintenance summary |
| Shared Site Build | Run tests | `.github/actions/site-build/action.yml:130-133` | test output | 降噪测试模拟日志 |
| Shared Site Build | Build site | `.github/actions/site-build/action.yml:135-140` | Hexo/build 输出 | 输出 file counts 和关键路径 |
| Deploy GitHub Pages | Purge cache | `.github/workflows/deploy-pages.yml:81-131` | 成功时 `cat response_file`；失败时 cat 最后响应 | 区分 deployStatus 与 cachePurgeStatus |
| Deploy GitHub Pages | Verify thought page | `.github/workflows/deploy-pages.yml:133-190` | retry 文本 | 结构化 attempts |
| Deploy CF Pages Dev | Deploy to Cloudflare Pages | `.github/workflows/deploy-cloudflare-pages-dev.yml:79-127` | wrangler 输出 | 保留 deploy URL，失败最近 N 行 |
| Deploy CF Pages Dev | Verify thought page | `.github/workflows/deploy-cloudflare-pages-dev.yml:129-180` | retry / propagation 文本 | 结构化 attempts |

## Markdown Backup

Workflow: `.github/workflows/markdown-backup.yml`

| Step | 代码位置 | 当前日志 | 优化方向 |
| --- | --- | --- | --- |
| Decide whether to run | `markdown-backup.yml:30-63` | 输出 gate outputs | 保留 |
| Export markdown from database | `markdown-backup.yml:82-84` | `npm run export:markdown` 完整 JSON | P0：compact summary |
| Detect backup changes | `markdown-backup.yml:86-95` | changed true/false | 保留，补 changed file count |
| Commit markdown backup | `markdown-backup.yml:97-105` | git 输出 | 保留 |
| Evaluate backup alert | `markdown-backup.yml:107-127` | warning annotation | 保留 |
| Write backup summary | `markdown-backup.yml:128-143` | summary | 补 export counts/duration |

## CI Tests

Workflow: `.github/workflows/ci-tests.yml`

| Job | 代码位置 | 当前日志 | 优化方向 |
| --- | --- | --- | --- |
| `test` | `ci-tests.yml:65-87` | `npm run test:fast` | 保留 |
| `full-test` | `ci-tests.yml:89-106` | schedule/manual 再跑 `npm test` | 避免与 fast test 重叠，或 summary 标注 test fixture logs |

## Refresh Telegram Webhook

Workflow: `.github/workflows/refresh-telegram-webhook.yml`

| Step | 代码位置 | 当前日志 | 优化方向 |
| --- | --- | --- | --- |
| Set Telegram webhook | `refresh-telegram-webhook.yml` | 输出 Telegram API result | 当前足够，补 webhook host 不补 token |

## C 类删除 / 降级项

| 原结论 | 当前复核结果 | 处理 |
| --- | --- | --- |
| ⚠️ Action 日志已打印原始 `DISPATCH_PAYLOAD` / `SYNC_DISPATCH_PAYLOAD` | **第三轮回退为真实成立（main 分支）**：`git show main:.github/workflows/sync.yml:89-91`、`sync-dev.yml:83-85` 仍写原文 env；实测 Sync Main #112 日志第 229/605/697/828 行可见。dev 已修复（`026ef49`）未合并 main | **回退为 P0**：合并 dev 修复到 main，校验日志无原文 |
| 同一批次重复调用 AI 是性能问题 | 仅发现 retry、response_format fallback、fallback provider 等有条件机制 | 不列为既有问题 |
| COS 存在重复上传 | 上传前 `HeadObject`，已存在计 `skipped` | 不列为既有问题 |
