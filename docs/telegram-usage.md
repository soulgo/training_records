# Telegram 使用说明

这份文档面向日常使用者，专门说明当前仓库里 Telegram Bot 的常见用法：怎么发图片、怎么发随想、怎么编辑随想、怎么删除随想、怎么移动随想，以及怎么使用分析和 AI 助手。

默认前提：

- 你是在已经授权的 Telegram chat 里和 Bot 交互
- 当前随想模块有三个：`锻炼`、`杂七杂八` 和 `身体反馈`

## 1. 你可以用 Telegram 做什么

当前主要有两类用法：

- 发训练/饮食/体脂截图，让系统自动识别并归档
- 发“随想”，把文字或带图内容写进站点随想页
- 发送 `/分析 问题` 获取训练建议
- 发送 `/ai 问题` 让 AI 助手调用 MCP 工具查询历史、同步状态或综合分析
- 发送 `/help`、`/帮助`、`help` 或 `帮助` 查看当前命令清单

这两类入口不要混淆：

- 普通图片消息会走截图识别
- 但如果图片 caption 以 `/随想` 或 `/thought` 开头，这批图片会被当作“随想”，不会再走训练截图识别
- `/help`、`/帮助`、`help`、`帮助`、`命令`、`指令`、`使用说明` 会优先由 Cloudflare Worker 直接回复，不会触发 GitHub Actions；如果消息已经进入 Telegram Sync，也会直接回发帮助，不写数据库或文件

## 2. 帮助命令

如果记不住命令，可以直接给 Bot 发：

```text
/help
/帮助
help
帮助
命令
指令
使用说明
```

Bot 会直接回发当前可用命令清单，包括截图、随想、编辑、删除、移动、`/分析` 和 `/ai`。

这类帮助消息优先在 Cloudflare Worker 层处理，不会触发 GitHub `repository_dispatch`，也不会启动 `Telegram Sync` GitHub Actions。如果 webhook 配置或部署版本导致消息已经进入 `Telegram Sync`，同步脚本也会直接回发帮助，不会写数据库、Markdown 或图片识别结果。

## 3. 图片怎么发

### 3.1 发送训练、饮食、体脂截图

最简单的方式就是直接把图片发给 Bot：

- 单张图片可以直接发
- 多张同一天的图片可以作为一个相册一起发
- 如果同一相册里混入了不同日期的图片，整批可能被跳过，不会强行错归档

归档日期的核心规则：

- 系统优先看图片里真实可见的日期
- 图片里没有可靠日期时，才可能回退到 Telegram 原始文件名日期
- 如果既没有图片日期，也没有可用文件名日期，这批图片会被跳过

发送方式建议：

- 如果你想依赖“文件名里的日期”做回退，优先用 Telegram `文件` 发送，不要用 `照片`
- 因为 `照片` 发送时，Telegram Bot API 通常拿不到原始文件名
- 如果只能用 `照片` 发送，尽量保证截图画面里本身就能看到日期

## 4. 发送随想

### 4.1 纯文字随想

可以直接给 Bot 发：

```text
/随想 今天深蹲动作轨迹更稳了
/thought 今天训练后臀部发力更明显
```

默认会进入 `锻炼` 模块。

### 4.2 指定随想模块

如果想把随想发到不同模块，可以在命令后面先写模块名：

```text
/随想 锻炼 今天腿练得很实
/随想 杂七杂八 今天把零碎事情记一下
/thought 杂七杂八 今天记一条非训练内容
/随想 身体反馈 今天硬拉后右侧腰背有点刺痛
```

当前支持的模块名只有：

- `锻炼`
- `锻炼随想`
- `杂七杂八`
- `身体反馈`

规则要点：

- 命令后的第一个词如果是上面这些模块名，会被当作模块选择，不会算进正文
- 如果第一个词不是已识别模块，就会被当成正文，模块仍然默认是 `锻炼`

### 4.3 带图随想

如果你想发“图片 + 随想”，做法是把命令写进图片 caption：

```text
/随想 今天深蹲轨迹更稳了
/thought 训练姿态记录
/随想 杂七杂八 今天把这张图顺手存一下
/随想 身体反馈 今天膝盖外侧有点酸，附上动作截图
```

说明：

- 单张图片支持
- 相册也支持
- 相册 caption 会生成一条随想，并把这组图片一起挂到同一条随想下
- 识别为随想后，这批图不会再进入训练截图 AI 识别

## 5. 怎么找到随想 ID

很多编辑、删除、移动命令都支持直接按 `id` 操作。

这个 `id` 就是随想页面上显示的 `#126`、`#175` 这种编号，也就是 Telegram message id。

如果你不知道 id，有两种最简单的办法：

- 去随想页面看卡片右侧的 `#数字`
- 或者直接回复原随想消息，不用手工填 id

## 6. 编辑随想

当前支持三种编辑方式。

### 6.1 直接编辑原 Telegram 消息

如果这条随想就是你自己刚发的原始 Telegram 消息，可以直接在 Telegram 里编辑它的内容。

适合场景：

- 改错别字
- 改正文
- 小幅修正文案

如果只是普通正文修订，这是最直接的方式。

### 6.2 回复原随想消息，发送修订版

回复原随想消息，再发一条新的修订命令：

```text
/随想 修订后的完整正文
/thought 修订后的完整正文
/随想 杂七杂八 修订后的完整正文
/随想 身体反馈 修订后的完整正文
```

规则：

- 这条新消息必须“回复”到原随想消息
- 如果没有显式写模块，系统会保留原来的模块
- 如果显式写了 `锻炼`、`杂七杂八` 或 `身体反馈`，系统会同步更新模块和标签

### 6.3 通过 ID 显式编辑

如果你不想回复原消息，也可以直接按 id 改：

```text
/随想编 126 修订后的完整正文
/随想编 126 杂七杂八 修订后的完整正文
/随想编 126 身体反馈 修订后的完整正文
```

这也是当前最适合“精确修改指定随想”的方式。

### 6.4 编辑时替换图片

如果你想把一条旧随想里的图片换掉，推荐使用：

```text
/随想编 126 修订后的完整正文
```

然后把新图片和这条命令一起发送。

这里要注意：

- 这是“替换图片”，不是“追加图片”
- 新图写入后，旧图会被删除
- 如果只是普通回复修订或直接编辑原消息，不建议依赖它来替图；要替图时优先用 `/随想编 <id> ...`

## 7. 删除随想

删除支持两种方式：

```text
/随想删
/随想删 126
```

规则：

- `/随想删` 必须回复原随想消息
- `/随想删 126` 是直接按 id 删除
- 删除时不需要写模块名
- 删除后会一起删掉对应的随想 Markdown 和关联图片

## 8. 移动随想

“移动”指的是把一条随想从一个模块改到另一个模块，比如从 `锻炼` 挪到 `杂七杂八`，或挪到 `身体反馈`。

支持两种方式：

```text
/移动 锻炼
/移动 杂七杂八
/移动 身体反馈
/移动 126 锻炼
/移动 126 杂七杂八
/移动 126 身体反馈
/随想 126 锻炼
/随想 126 杂七杂八
/随想 126 身体反馈
```

规则：

- `/移动 锻炼`、`/移动 杂七杂八`、`/移动 身体反馈` 需要回复原随想消息
- `/移动 126 锻炼`、`/移动 126 杂七杂八`、`/移动 126 身体反馈` 是直接按 id 移动
- `/随想 126 锻炼`、`/随想 126 杂七杂八`、`/随想 126 身体反馈` 也会按移动处理，兼容以前的发送习惯
- 移动只会改模块和标签
- 不会改正文、图片、日期、文件名和随想 id

## 9. 分析与 AI 助手

### 9.1 `/分析` 训练建议

```text
/分析 今天怎么练
/analysis 最近7天训练怎么样
```

`/分析` 是专用训练分析命令，会读取训练、体脂、饮食和身体反馈摘要，生成训练建议。它不会写 Markdown、随想或数据库。

### 9.2 `/ai` MCP Agent 助手

```text
/ai 搜一下右肩疼痛相关记录
/ai 同步状态正常吗
/智能助手 有哪些命令可以用
```

`/ai` 是通用助手入口，会按问题调用 MCP 工具。它适合查询历史、同步状态、配置、身体反馈，或做跨模块综合分析。它同样不会写 Markdown、随想或数据库。

## 10. 常见建议

- 发训练截图时，尽量一批只发同一天的图，不要跨天混发
- 想靠文件名日期回退时，用 Telegram `文件` 发送，不要用 `照片`
- 想发“带图随想”时，记得把 `/随想` 或 `/thought` 写在 caption 开头
- 想精确修改某条随想时，优先用 `/随想编 <id> ...`
- 想删除或移动，但一时找不到 id 时，直接回复原随想消息最省事
- 如果正文开头正好是 `锻炼`、`杂七杂八` 或 `身体反馈`，系统可能把它当成模块名；这种场景建议把表达方式改一下，避免歧义

## 11. 相关文档

如果你想看更偏维护视角的说明，可以继续看：

- `docs/thoughts-module.md`：随想模块、Markdown、数据库镜像的维护说明
- `docs/telegram-date-resolution.md`：截图日期归档规则，尤其是 `照片` 和 `文件` 的差异
- `docs/telegram-analysis.md`：`/analysis` / `/分析` 指令说明
