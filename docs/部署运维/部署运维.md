# 部署运维

本文是 GitHub Actions、Cloudflare Workers、Pages、Telegram、飞书、main/dev 环境和 Secrets/Variables 部署配置的唯一入口。


## 部署运维总览

部署涉及 GitHub Actions、Cloudflare Worker、Telegram、飞书、GitHub Pages 和 Cloudflare Pages。

## 基本原则

- main 和 dev 隔离。
- 外部平台配置需要人工确认。
- 仓库只能证明 workflow、wrangler、代码读取哪些配置，不能证明平台当前值。


## GitHub Actions

## 主要 workflow

| Workflow | 作用 |
| --- | --- |
| `.github/workflows/ci-tests.yml` | 测试 |
| `.github/workflows/sync.yml` | main 消息同步 |
| `.github/workflows/sync-dev.yml` | dev 消息同步 |
| `.github/workflows/deploy-pages.yml` | 生产 Pages 部署 |
| `.github/workflows/deploy-cloudflare-pages-dev.yml` | dev Pages 预览 |
| `.github/workflows/markdown-backup.yml` | DB -> Markdown 备份 |
| `.github/workflows/deploy-cloudflare-worker.yml` | main Worker 部署 |
| `.github/workflows/deploy-cloudflare-worker-dev.yml` | dev Worker 部署 |
| `.github/workflows/refresh-telegram-webhook.yml` | Telegram webhook 刷新 |

## 排障口径

先看 workflow 是否运行，再看业务 summary。绿色 workflow 仍可能包含 `partialFailure`、`pending_replay` 或 `manual_intervention`。

消息同步 workflow 的业务字段优先级：

- `taskStatus`
- `persistenceStatus`
- `failureDisposition`
- `queue_task_id`
- `payload_hash` / `payloadHash`

## 队列与重跑

- Worker 的 `SyncDispatchQueue` 触发 `.github/workflows/sync.yml` 或 `.github/workflows/sync-dev.yml`。
- `queue_task_id` 用于关联 Worker 队列任务和 workflow run。
- workflow run 查找超时 / run lookup timeout 后，应检查 dead-letter 和 Worker 日志。
- 重跑 / rerun 必须保持幂等；同一 `batch_id + payload_hash` 应返回 `unchanged`，不得重复写 core。


## Cloudflare Workers 部署

## 文件

- `cloudflare/sync-dispatch-worker.mjs`
- `cloudflare/telegram-sync-dispatch-worker.mjs`
- `cloudflare/feishu-sync-dispatch-worker.mjs`
- `cloudflare/sync-dispatch-queue.mjs`
- `wrangler.toml`
- `wrangler.dev.toml`

## 绑定

- `TELEGRAM_ALBUM_BUFFER`
- `FEISHU_IMAGE_BUFFER`
- `SyncDispatchQueue`

## 部署验证

```bash
npx wrangler deploy --dry-run --config wrangler.toml
npx wrangler deploy --dry-run --config wrangler.dev.toml
```

真实部署和 secret 状态需要 Cloudflare 控制台或 wrangler 实际环境确认。


## GitHub Pages 与 Cloudflare Pages

## 生产

生产站点由 GitHub Pages 发布。`source/CNAME` 指向生产域名。

## Dev

dev 预览由 Cloudflare Pages 发布，构建时应使用 dev 数据库配置，并删除 `public/CNAME`，避免覆盖生产域名。

## 页面没有更新时

先判断是数据未写入、构建未执行、部署失败，还是 CDN 缓存。


## Telegram 配置

## 必要配置

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_ALLOWED_CHAT_IDS`
- `TELEGRAM_SECRET_TOKEN`
- Telegram webhook URL

## 通道能力

- 图片/相册识别。
- 随想新增、编辑、删除、移动。
- Markdown 文档附件随想。
- `/analysis`。
- 帮助命令。

## 验证

发送 `/help`，确认 Worker 或同步脚本能回复。再发送一张有清晰日期的测试截图，查看 Actions summary 和数据库结果。


## 飞书配置

## 必要配置

飞书开放平台：

- App ID
- App Secret
- Encrypt Key
- Verification Token
- 事件订阅 `im.message.receive_v1`
- 权限 `im:message`、`im:message.p2p_msg`、`im:message.group_at_msg`、`im:resource`

GitHub Actions：

- `FEISHU_APP_ID`
- `FEISHU_APP_SECRET`
- `FEISHU_ALLOWED_CHAT_IDS`

Cloudflare Worker：

- `FEISHU_ENCRYPT_KEY`
- `FEISHU_VERIFICATION_TOKEN`
- `GITHUB_TOKEN`

## 验证

1. 飞书 Request URL challenge 成功。
2. `/帮助` 能回复。
3. `/随想` 能写 `core.thought`。
4. 有日期图片能写 `core.*` 且 `source_channel='feishu'`。


## main/dev 环境矩阵

| 项 | main | dev |
| --- | --- | --- |
| 同步 workflow | `sync.yml` | `sync-dev.yml` |
| Worker 配置 | `wrangler.toml` | `wrangler.dev.toml` |
| Telegram dispatch | `telegram_update` | `telegram_update_dev` |
| 飞书 dispatch | `feishu_update` | `feishu_update_dev` |
| 数据库 | `TRAINING_DB_URL` | `DEV_TRAINING_DB_URL` |
| Bot token | `TELEGRAM_BOT_TOKEN` | `DEV_TELEGRAM_BOT_TOKEN` 或独立 dev 配置 |
| 飞书配置 | `FEISHU_*` | `DEV_FEISHU_*` 可覆盖 |

## 原则

- dev 不能误写 main 数据库。
- dev Request URL 不能填生产 Worker。
- main deploy 不能使用 dev CNAME 或 dev DB。


## Secrets 与 Variables

## 分类

| 类别 | 示例 |
| --- | --- |
| 通道 | `TELEGRAM_BOT_TOKEN`、`FEISHU_APP_ID` |
| AI | `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL` |
| 数据库 | `TRAINING_DB_URL`、`DEV_TRAINING_DB_URL` |
| Worker | `GITHUB_TOKEN`、`TELEGRAM_SECRET_TOKEN`、`FEISHU_ENCRYPT_KEY` |
| Pages | `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID` |

## 维护规则

- 文档只记录变量名和用途，不记录 secret 值。
- GitHub Secret 值不可从仓库反读。
- Cloudflare Worker secret 需要在目标环境单独配置。
- 平台当前值需要人工确认。
- 完整变量清单维护在 [环境变量索引](../参考资料/参考资料.md#环境变量索引)。
