# 系统导览

本文是系统上下文、当前边界和阅读路径的唯一总览入口。


## 10 分钟系统导览

这个项目是个人训练记录系统。它把 Telegram 和飞书里的训练截图、饮食截图、体脂秤截图、睡眠截图、随想和分析请求，转成 PostgreSQL 中的结构化训练数据，再生成静态训练看板。

## 一句话架构

```text
Telegram / 飞书
-> Cloudflare Worker
-> SyncDispatchQueue / GitHub Actions
-> 统一消息同步 Use Case
-> AI 识别或分析
-> PostgreSQL core.* / ingest.*
-> 随想图片本地 artifact 或腾讯云 COS URL
-> Markdown 备份与 Hexo 静态站点
```

## 系统为什么存在

目标不是做一个通用 OCR 工具，而是把个人训练相关事实长期、可恢复、可分析地存起来：

- 训练数据：活动、热量、时长、距离、心率。
- 饮食数据：餐次、摄入、建议范围。
- 体脂秤数据：体重、BMI、体脂率、骨骼肌、水分、基础代谢。
- 睡眠数据：入睡、醒来、睡眠阶段、评分、心率、HRV、血氧。
- 随想系统：训练感受、身体反馈、杂项记录，支持编辑、删除、移动。
- 分析系统：读取现有事实生成训练建议，不写入事实数据。

## 当前事实源

PostgreSQL 是事实源：

- `core.*` 保存业务主数据。
- `ingest.*` 保存原始消息、识别、pending、AI 调用审计。
- `archive.*` 保存构建快照和历史解析留痕。

Markdown 是派生备份，不是正常图片同步的主写入路径。

随想图片引用也是数据库事实的一部分。默认未启用 COS 时，新图片保存为本地 `/images/thoughts/...` 路径；启用腾讯云 COS 后，新图片上传到 COS，`core.thought.image_refs_json` 保存完整公有读 URL。历史本地图片路径不迁移，仍可和新 COS URL 共存。

## 消息怎么进系统

Telegram 和飞书都进入统一 Cloudflare Worker。Worker 验证来源后，通过队列触发 GitHub Actions。Actions 根据 channel 执行 `npm run sync:telegram` 或 `npm run sync:feishu`。

代码当前已经有统一编排入口 `runMessageSync()`；飞书仍通过 adapter 复用 Telegram 同步主链路，这是历史命名债，不代表业务上只有 Telegram。

## AI 做什么

AI 只负责把图片中可见事实转成结构化 JSON。系统必须再校验：

- JSON/schema 是否有效。
- `imageType` 是否属于 `measurement`、`workout`、`nutrition`、`sleep`、`unknown`。
- `confidence` 是否足够。
- 日期证据是否可靠。
- 业务字段是否可入库。

AI 不能用历史记录、目标值、常识或当前日期补齐截图没有显示的事实。

## 页面怎么生成

页面构建读取 `core.*`，生成 `source/_data/training.json` 和 `source/_data/dashboardView.json`，再由 Hexo 渲染成静态站点。GitHub Pages 发布生产站点，dev 可发布 Cloudflare Pages 预览。

## 失败怎么处理

Action 绿色只代表 workflow 执行成功，不代表业务成功。判断业务结果要看：

- `taskStatus`
- `persistenceStatus`
- `failureDisposition`
- `dateSources`
- `warnings`
- pending / dead-letter / summary

数据库失败通常进入 pending replay；低置信度、缺少可靠日期、非目标图片通常需要人工介入或重新发送。

## 下一步读什么

- 想理解业务：读 [核心业务总览](../核心业务/核心业务.md)。
- 想理解用户输入会发生什么：读 [系统行为手册](系统行为手册.md)。
- 想理解链路：读 [消息链路总览](../消息链路/消息链路.md)。
- 想改代码：读 [开发指南](../开发指南/开发指南.md)。
- 想排障：读 [故障排查](../故障排查/故障排查.md)。
- 让 AI Agent 接手：读 [AI Agent 入口](../AI-Agent/README.md)。


## 系统上下文

系统服务的是个人训练记录的长期维护，而不是一次性截图 OCR。

## 核心参与方

| 参与方 | 作用 |
| --- | --- |
| 用户 | 通过 Telegram 或飞书发送截图、随想、分析命令 |
| Cloudflare Worker | 验证 webhook、聚合图片 burst、触发同步任务 |
| GitHub Actions | 执行同步、构建、部署、备份和维护脚本 |
| AI Provider | 识别图片或生成训练分析 |
| PostgreSQL | 保存主数据、审计、pending、归档 |
| Hexo / Pages | 生成和发布静态训练看板 |
| Markdown | 人工可读备份和显式恢复入口 |

## 系统核心价值

1. 把训练相关事实稳定写入结构化数据库。
2. 保留外部消息和 AI 调用审计，便于追溯。
3. 当 AI、数据库、Action 或通道异常时，不静默丢数据。
4. 把页面展示、Markdown 备份和训练分析建立在同一份事实源上。

## 业务优先级

- 准确性优先于入库率。
- 可恢复性优先于链路简短。
- 业务状态优先于 Action 红绿状态。
- 双通道一致性优先于历史命名。


## 当前状态与边界

本文定义当前系统事实和不可轻易修改的边界。

## 当前状态

- main/dev 的 Telegram 和飞书都进入统一 Cloudflare Worker。
- 消息同步由 GitHub Actions 执行，生产用 `sync.yml`，dev 用 `sync-dev.yml`。
- 应用层已有 `runMessageSync()` 统一编排；`runFeishuSync()` 通过飞书 adapter 复用同步主链路。
- 图片识别支持 `measurement`、`workout`、`nutrition`、`sleep`、`unknown`。
- `core.*` 是事实源；Markdown 是 DB 派生备份。
- 随想图片存储支持本地 artifact 和腾讯云 COS；COS 启用后由 GitHub Actions 上传，Cloudflare Worker 不接触 COS 凭据。
- pending 默认使用 PostgreSQL `ingest.telegram_pending_batch`；legacy NDJSON 只在显式开关下重放。
- AI 调用支持 OpenAI-compatible provider、超时、重试、识别 fallback 和 best-effort 审计日志。

## 不可破坏能力

- Telegram 和飞书双通道采集。
- 训练、饮食、体脂秤、睡眠四类图片识别入库。
- 低置信度、缺少可靠日期、非目标图片不得强行入库。
- 随想新增、编辑、删除、移动和图片附件。
- Telegram Markdown 文档附件随想。
- `/analysis` 只读分析，不写事实数据。
- Markdown 导入是显式维护入口，具有整日替换语义。
- DB pending replay、幂等、事务和来源审计。
- DB -> Markdown 备份和 Hexo 静态站点生成。

## 当前命名债

- `telegram_*` 表和部分函数承载通用消息同步审计，飞书通过 `source_channel='feishu'` 区分来源。
- `core.thought.telegram_message_id` 是 legacy 兼容字段，跨通道主身份应看 `source_channel/source_chat_id/source_message_id`。
- 许多历史文档仍以 Telegram 为中心；新文档按业务能力和消息通道分层。

## 文档边界

- 正式事实写入 `总览/`、`架构/`、`核心业务/`、`消息链路/`、`AI识别体系/`、`数据模型/`、`部署运维/`、`开发指南/`、`运维手册/`、`故障排查/`、`参考资料/`、`AI-Agent/`。
- 历史重构方案只保留在 `归档/`。
- 不再在 `归档/` 中维护“当前状态”。

## 判断完成的证据

一个变更不能只看“文档已写”。必须能指向：

- 代码或 SQL 的事实源。
- 对应文档的唯一 SSOT 位置。
- 必要时对应测试或验证命令。
- 若是外部配置，明确标注“仓库可确认”或“需平台人工确认”。


## 文档地图

## 新人

1. [10 分钟系统导览](#10-分钟系统导览)
2. [系统行为手册](系统行为手册.md)
3. [当前状态与边界](#当前状态与边界)
4. [核心业务总览](../核心业务/核心业务.md)
5. [故障排查总览](../故障排查/故障排查.md)

## 架构师

1. [系统架构](../架构/系统架构.md)
2. [消息同步架构](../消息链路/消息链路.md)
3. [数据源策略](../数据模型/数据模型.md#数据源策略)
4. [ADR](../架构/架构决策记录/README.md)

## 开发人员

1. [项目结构](../开发指南/开发指南.md#项目结构)
2. [核心模块](../开发指南/开发指南.md#核心模块)
3. [调试方式](../开发指南/开发指南.md#调试方式)
4. [测试方式](../开发指南/开发指南.md#测试方式)
5. [端到端验收标准](../开发指南/开发指南.md#端到端验收标准)
6. [内部接口](../参考资料/参考资料.md#内部接口索引)

## 运维人员

1. [部署运维总览](../部署运维/部署运维.md)
2. [日常维护](../运维手册/运维手册.md#日常维护)
3. [pending 重放](../运维手册/运维手册.md#pending-replay)
4. [数据恢复](../运维手册/运维手册.md#数据恢复)
5. [故障排查](../故障排查/故障排查.md)

## AI Agent

1. [AI Agent 入口](../AI-Agent/README.md)
2. [系统行为手册](系统行为手册.md)
3. [任务路由](../AI-Agent/README.md#任务路由)
4. [事实与不变量](../AI-Agent/README.md#事实与不变量)
5. [安全改动边界](../AI-Agent/README.md#安全改动边界)
6. [验证命令](../AI-Agent/README.md#验证命令)
