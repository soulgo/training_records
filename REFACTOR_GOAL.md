# Goal Document: 系统代码终极重构优化

## Go / No-Go

- **Judgment**: Go
- **Reason**: 目标、禁止事项和最终交付物已明确；所有架构判断与删除动作都设置了“真实代码证据先行”的阶段门禁。

## Target Outcome

在保留现有业务能力的前提下，把当前系统重构为来源无关、可扩展、可安全部署迁移的 AI 截图识别系统；最终由真实代码、数据库迁移、自动化测试与 `REFACTOR_REPORT.md` 共同证明结果。

## Goal Definition

- **Type**: technical / quality / delivery
- **Boundary**: 包含前后端、AI/OCR/图片处理、数据处理、API、数据库访问、配置、部署和自动化脚本；`docs` 仅作历史证据和背景参考，不作为重构主体。
- **Non-goals**:
  - 不基于历史文档虚构当前代码中不存在的业务或模块。
  - 不为未知的外部消费者保留无证据的兼容层。
  - 不在缺少调用关系、测试或运行证据时删除代码。
  - 不把平台迁移等同于本次立即上线到某个指定云平台。
- **Deferred work**:
  - 真实生产环境的最终发布、流量切换和第三方账户配置，除非仓库已有可安全执行的本地流程。
  - 需要业务方选择但无法从代码、数据或配置推断的产品语义变化。
- **Verification rule**: 每一阶段都必须产出可复核证据；最终要求测试/静态检查/构建通过，迁移 SQL 可校验，关键识别流程有回归测试，旧接口删除有调用关系证据，最终报告逐项关联真实文件。
- **Evidence source**: 仓库代码、Git 历史与分支差异、数据库 schema/ORM/SQL、配置和部署文件、测试与构建命令、静态搜索、运行日志或本地行为验证。
- **Pass criteria**:
  - 完成全部代码、数据库、配置和部署面的审计并形成带文件路径的分析报告。
  - 通用识别流程不再以单一 App 固定布局为核心扩展机制，图片处理、OCR、AI 理解、标准化职责可独立替换和测试。
  - 已证实无调用或废弃的兼容接口被删除，重复业务路径被收敛，现有受支持行为通过回归验证。
  - dev/main 配置、数据和密钥边界明确，仓库无新增明文密钥或硬编码凭据。
  - 必要数据库变更提供可执行且可审查的 `sql/migration.sql`，并说明原因和影响。
  - 生成完整 `REFACTOR_REPORT.md`，记录每项修改的文件、内容、原因、影响和验证证据。
- **Confidence note**: 自动化测试和构建是主要证据；仓库缺少测试的区域将补充契约/回归测试，并明确剩余无法本地证明的风险。
- **Judgment owner**: 自动化验证结果负责技术通过；涉及无法从仓库判断的业务语义时由用户最终确认。

## Current State

- 用户指出当前识别逻辑主要面向华为 App 固定页面、字段和 UI，但该判断需要在 Phase 1 用真实代码验证。
- 当前代码结构、数据库类型、运行栈、测试覆盖、分支配置差异和部署方式尚未完成审计，不在目标文档中预设答案。
- 最高风险是大范围改造造成行为回归、错误删除隐式调用代码、数据库迁移不可逆，以及把文档设想误当成当前实现。

## Plan Rewrite Notes

| Existing item | Decision | Reason |
|---------------|----------|--------|
| 完整阅读当前系统 | keep / move first | 是所有后续判断的证据基础，阶段内禁止修改生产代码。 |
| 当前系统代码分析 | merge with baseline audit | 与完整阅读共同形成可复核基线，避免重复扫描。 |
| AI 识别能力重构 | reorder after architecture decision | 先确认真实识别链、数据契约与测试边界，再实施模块拆分。 |
| 删除旧兼容接口 | rewrite as evidence-gated cleanup | “看起来旧”不足以删除，必须证明无调用或明确废弃。 |
| 系统架构解耦 | merge with target architecture | 以当前依赖和部署约束为依据，不机械创建层级。 |
| dev/main 双环境优化 | keep | 与配置、密钥和数据隔离一起验证，避免用分支承载秘密。 |
| 数据库优化 | keep / gate by migration proof | 先审计 schema 和真实查询，再决定是否生成迁移。 |
| 系统安全优化 | move earlier into every phase | 安全不是末尾补丁，审计、设计和实施阶段都要设置门禁。 |
| 代码质量优化 | merge into implementation rules | 日志、异常、职责和复用随相关模块重构，不单独做无边界清扫。 |
| 代码修改流程 | keep / strengthen verification | 保留“分析→方案→修改”，为每阶段增加测试和停止条件。 |

## Drift Diagnosis

- **Goal drift**: 原计划的主题都与目标相关，但部分主题缺少可证明的退出条件。
- **Phase drift**: 原十阶段主要按主题排列；重排后按证据、风险、依赖和可回滚切片推进。
- **Validation drift**: 原计划强调修改内容，现补充测试、构建、迁移校验、调用关系和行为证据。
- **Compatibility drift**: 原计划要求删除兼容层，但未定义公共契约；现改为证据门禁，公共消费者未确认时暂停删除。
- **Cleanup drift**: 全面清理容易扩大范围；只处理会阻碍目标架构、安全或维护性的技术债。

## Priority Rationale

- 先锁定真实运行面和行为基线，避免把历史文档、分支约定或命名误读为现状。
- 先处理高不确定性的 AI 数据契约、持久化结构和公共 API 边界，再进行大规模搬移和删除。
- 数据迁移、安全与配置隔离贯穿实现，避免在重构结束后才发现不可部署或不可回滚。

## Assumptions and Open Decisions

| Item | Status | Impact | Owner / Next step |
|------|--------|--------|-------------------|
| 华为固定规则是当前核心识别路径 | assumed | 决定 AI 重构中心 | Phase 1 通过代码与测试确认。 |
| 仓库内可获得数据库真实结构或迁移历史 | assumed | 决定数据库审计可信度 | 搜索 ORM、schema、SQL、容器和连接配置。 |
| dev/main 分支均可本地读取 | unresolved | 决定配置差异审计范围 | 检查 Git 分支与工作树，不切换或覆盖用户改动。 |
| 外部 API 消费者可由仓库和历史证据识别 | unresolved | 决定旧接口能否删除 | 建立接口清单、调用图和 Git 证据；不足则暂停删除。 |
| AI/OCR 第三方服务可在无真实密钥时测试 | unresolved | 决定端到端验证深度 | 使用契约测试、mock/fixture；真实调用仅在已有安全环境允许时进行。 |

## Phases

### Phase 1: 真实系统基线与完整审计

- **Purpose**: 建立当前架构、调用关系、数据流、数据库、配置、部署和测试的事实底图。
- **Entry condition**: 目标文档已建立，工作树状态已记录。
- **Phase rules**:
  - 只读生产代码、数据库定义、配置和历史资料；除分析/目标文档外不修改实现。
  - `docs` 只用于交叉验证，不作为事实优先级高于代码、schema 和配置。
  - 检查 dev/main 时不执行破坏性切换，不覆盖用户未提交改动。
- **Todos**:
  - [ ] 清点全部源代码、配置、SQL、部署、脚本、测试与生成物边界。
    - **Surface**: repository / git
    - **Proof**: 文件分类清单与排除规则。
    - **Depends on**: none
  - [ ] 逐模块阅读前端、后端、AI/OCR、图片处理、数据访问和 API。
    - **Surface**: code
    - **Proof**: 架构图、入口/调用关系、关键文件路径。
    - **Depends on**: 文件清单
  - [ ] 审计全部表、字段、类型、索引、关系、ORM 和 SQL。
    - **Surface**: database
    - **Proof**: schema 对照表与真实查询引用。
    - **Depends on**: 文件清单
  - [ ] 比较 dev/main 配置、环境变量、密钥边界和部署差异。
    - **Surface**: git / config / deployment
    - **Proof**: 脱敏差异报告。
    - **Depends on**: 工作树安全确认
- **Exit proof**: 形成带具体文件路径和证据的当前系统代码分析报告，覆盖用户指定的所有阅读范围。
- **Stop condition**: 发现无法安全读取的外部系统、工作树冲突，或关键事实只能依赖用户未提供的信息。

### Phase 2: 目标架构与迁移设计

- **Purpose**: 基于 Phase 1 事实确定最小但完整的目标架构、契约和迁移顺序。
- **Entry condition**: Phase 1 报告完成且关键运行路径可解释。
- **Phase rules**:
  - 不为“看起来更分层”创建空壳模块；每个边界必须隐藏真实复杂度或降低变化扩散。
  - 明确受支持 API/数据契约、删除候选、回滚点与数据库迁移策略。
  - 方案必须包含现状到目标的增量落地顺序和每步验证。
- **Todos**:
  - [ ] 设计图片处理→OCR→AI理解→标准化→业务保存的契约与错误边界。
  - [ ] 设计 Frontend/API/Business/AI/Database/Storage 的实际依赖方向。
  - [ ] 制定旧接口清理、配置隔离、安全、日志和数据库迁移清单。
- **Exit proof**: 重构方案逐项映射到真实文件、风险、测试和迁移步骤。
- **Stop condition**: 目标契约需要未决业务语义，或无法定义兼容/回滚策略。

### Phase 3: 受测试保护的核心识别重构

- **Purpose**: 建立通用截图识别流水线并保留当前已支持行为。
- **Entry condition**: 目标契约和回归样例已确认。
- **Phase rules**:
  - 严格使用 red-green-refactor；先证明现状或目标行为，再修改实现。
  - 外部 AI/OCR 调用通过适配器和契约隔离，日志不得泄漏图片内容或密钥。
  - 不引入“一 App 一解析器”作为默认扩展机制；必要的来源提示必须是可配置证据而非硬编码主路径。
- **Todos**:
  - [ ] 拆分并实现图片处理、OCR、AI理解、标准化职责及稳定数据契约。
  - [ ] 把现有华为识别能力迁移为通用流程的回归样例或来源提示。
  - [ ] 建立不同尺寸、布局、字段名称和失败路径的测试集。
- **Exit proof**: 新旧核心行为回归通过，通用输入契约与失败语义有自动化测试。
- **Stop condition**: 真实业务字段无法无损映射，或供应商能力与目标契约冲突。

### Phase 4: API、业务与旧代码收敛

- **Purpose**: 收敛重复路径，删除已证实废弃的接口和兼容逻辑，统一参数验证与异常返回。
- **Entry condition**: 新核心流程稳定且调用方清单完成。
- **Phase rules**:
  - 删除必须附调用证据、替代路径和回归验证。
  - 公共契约不明确时先标记并停止删除，不用猜测创建兼容层。
  - 合并重复逻辑时保留可观察行为。
- **Todos**:
  - [ ] 迁移前后端调用方到唯一受支持接口。
  - [ ] 删除无调用、废弃和临时路径，记录删除理由。
  - [ ] 统一校验、错误模型、请求/处理日志和追踪标识。
- **Exit proof**: 调用图无悬空引用，接口契约测试和前后端构建通过。
- **Stop condition**: 发现仓库外消费者或无法验证的外部契约。

### Phase 5: 数据库、配置、安全与部署迁移能力

- **Purpose**: 让数据模型、秘密管理和运行配置支撑新架构并降低平台绑定。
- **Entry condition**: 应用层目标字段和持久化需求稳定。
- **Phase rules**:
  - 数据库变更必须可执行、可审查并说明数据迁移/回滚风险。
  - 密钥只来自运行时秘密/环境注入；示例配置只能使用占位符。
  - 路径、存储、数据库和供应商配置通过边界注入，不写死单一平台。
- **Todos**:
  - [ ] 优化必要的表、字段、关系和索引并生成 `sql/migration.sql`（仅在确有变更时）。
  - [ ] 实现 dev/main 配置、数据和密钥隔离及启动时校验。
  - [ ] 抽象实际存在的平台路径/存储依赖，验证本地与容器化运行入口。
  - [ ] 完成敏感数据、文件访问、权限和日志脱敏检查。
- **Exit proof**: 迁移校验、配置测试、安全扫描、构建/启动验证通过。
- **Stop condition**: 迁移存在不可接受的数据丢失风险，或生产秘密/基础设施决策缺失。

### Phase 6: 全量验收与交付

- **Purpose**: 证明系统行为、迁移和维护性目标已达到，并明确剩余风险。
- **Entry condition**: 所有计划修改完成且局部验证通过。
- **Phase rules**:
  - 执行仓库允许的全量测试、构建、静态检查和迁移演练。
  - 报告只陈述有文件、命令或测试证据的结果。
  - 未验证项必须明确列为风险，不包装为完成。
- **Todos**:
  - [ ] 执行全量回归、构建、静态检查、安全与迁移验证。
  - [ ] 检查未跟踪文件、diff 和意外行为变化。
  - [ ] 生成 `REFACTOR_REPORT.md`，覆盖九项最终验收内容和逐文件修改记录。
- **Exit proof**: 最终验证通过，报告完整，剩余风险和后续动作明确。
- **Stop condition**: 关键测试失败、迁移不可复现或存在未解释的行为回归。

## Dry-Run Findings

- 原计划缺少“哪些接口属于公共契约”的证据来源，已增加调用图和仓库外消费者停止条件。
- AI 供应商与真实密钥可能无法在本地调用，已把契约测试/fixture 设为最低可证明路径，真实端到端调用不作为无条件前置。
- 数据库结构未知时不能预先承诺一定生成迁移；只有真实审计证明需要变更时才创建 `sql/migration.sql`。
- dev/main 是配置分支还是部署分支尚未验证；先做脱敏 Git 差异审计，不直接切换工作树。
- 大范围重构必须按垂直切片验证，不能在一次无保护搬移后统一补测试。

## Final Validation

- 运行仓库定义的全部测试、构建、lint/typecheck/static analysis。
- 对通用识别主流程执行多来源/布局 fixture 回归和失败路径验证。
- 校验数据库迁移可执行性、索引与 ORM/schema 一致性。
- 扫描硬编码秘密、绝对路径、平台绑定和已删除 API 的残留引用。
- 审查最终 diff 与 `REFACTOR_REPORT.md` 的逐项证据映射。

## First Execution Step

记录 Git 工作树与分支状态，生成完整文件清单和分类边界；随后只读检查入口、依赖、数据库、配置和部署文件。
